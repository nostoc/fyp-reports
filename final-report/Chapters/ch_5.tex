\chapter{Testing and Performance Evaluation}
\label{ch:testing}

\section{Introduction}
\label{sec:testing-introduction}

This chapter presents how the prototype described in Chapters~\ref{ch:architecture} and~\ref{ch:implementation} has been verified. Automated tests have been written alongside the implementation for every major component. A dedicated benchmark script has also been written to measure blockchain throughput, latency, and gas cost directly against the deployed private network.

Figure~\ref{fig:evaluation-structure} shows how the evaluation is organized. Functional correctness, Sybil-resistance and security, service integration, and blockchain performance are each covered by a dedicated test suite. 

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.95\textwidth,keepaspectratio]{Images/Chapter 5/evaluation-structure.png}
    \caption{Structure of the prototype evaluation}
    \label{fig:evaluation-structure}
\end{figure}

Figure~\ref{fig:test-suite-composition} shows how the total of 128 test cases are distributed across the four executed suites.

\begin{figure}[!htb]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            width=0.82\textwidth,
            height=6cm,
            bar width=26pt,
            symbolic x coords={Smart Contracts,AI Oracle,Relayer,IPFS},
            xtick=data,
            nodes near coords,
            ymin=0, ymax=85,
            ylabel={Passing test cases},
            enlarge x limits=0.25,
            axis lines*=left,
            clip=false,
            every node near coord/.append style={font=\small},
        ]
        \addplot[fill=barblue] coordinates {(Smart Contracts,75) (AI Oracle,20) (Relayer,25) (IPFS,8)};
        \end{axis}
    \end{tikzpicture}
    \caption{Test case distribution across suites}
    \label{fig:test-suite-composition}
\end{figure}

This chapter is focused on indicating the tests that were run and their results. More details on tests can be found in the Appendix~\ref{app:test-cases}. These results are discussed in the Chapter~\ref{ch:results}. 

\section{Evaluation Aims and Questions}
\label{sec:evaluation-aims}

The evaluation has been guided by the following questions. 

\begin{enumerate}
    \item Do the four smart contracts enforce their intended functions and access rules correctly?
    \item Are duplicate actions, replay attempts, invalid signatures, and emergency-report abuse rejected as designed?
    \item Are the relayer, queue, AI services, IPFS service, and blockchain correctly integrated into one working pipeline?
    \item What response time, throughput, block interval, and gas cost does the private network offer for a civic-reporting workload?
\end{enumerate}

The remaining sections of this chapter answer these questions in turn.

\section{Evaluation Environment}
\label{sec:evaluation-environment}

The prototype has been deployed on two cloud \ac{VPS}s, summarized in Table~\ref{tab:evaluation-environment}. By splitting the deployment with contract and blockchain traffic on one host and AI inference on a second, keeps the AI workload from competing with the block-sealing nodes as reasoned in Chapter~\ref{ch:implementation}.

\begin{table}[htbp]
\centering
\caption{Evaluation environment}
\label{tab:evaluation-environment}
\begin{tabular}{p{0.31\textwidth}|p{0.57\textwidth}}
    
    Item & Configuration \\
    \hline
    Server hardware & 4 vCPU cores, 8\,GB RAM, and 75\,GB SSD \\
    \hline
    Operating system & Ubuntu Server 22.04 LTS \\
    \hline
    \ac{VPS} 1 & Private PoA chain, relayer, Redis, identity simulator, and web application \\
    \hline
    \ac{VPS} 2 & Central AI aggregator with three classifier services, and the IPFS service \\
    \hline
    Blockchain & Chain ID 1337, three validators plus one RPC node, and a five-second Clique period \\
    \hline
    Contract tooling & Solidity 0.8.20, Hardhat, and Chai \\
    
\end{tabular}
\end{table}

The unit and integration test suites are run against the same technology stack, which means the recorded behaviour reflects the actual deployed configuration rather than a simplified test-only setup.

\section{Smart-Contract Functional Testing}
\label{sec:contract-testing}

\subsection{Method}

Contract behaviour has been tested with Hardhat and Chai on a local, fast in-process blockchain, which lets every test run quickly while still executing the real compiled contract bytecode. Each test deploys a fresh contract instance, so that no test can be influenced by state left behind by an earlier one. Hardhat's time-manipulation helpers has been used to advance the simulated chain clock directly, where a test needs a voting window or a penalty period to expire, rather than waiting in real time. Therefore, a six-hour voting window or a 30-day penalty can be exercised in milliseconds.

Table~\ref{tab:contract-inventory} summarizes the five test files and responsibility of each file.

\begin{table}[htbp]
\centering
\caption{Smart-contract test suite}
\label{tab:contract-inventory}
\begin{tabular}{p{0.32\textwidth}|p{0.10\textwidth}|p{0.46\textwidth}}
    
    Test file & Cases & Coverage \\
    \hline
    Reporting.ts & 41 & Submission, validation voting, finalization, authority actions, verification, rejection review, pagination, and query functions \\
    \hline
   EmergencyReporting.ts & 13 & Immediate opening, authority resolution, reclassification, and the 30-day penalty \\
    \hline
    Security.ts & 7 & Nullifier replay, access control, and Sybil-resistance across all four contracts \\
    \hline
   OpinionPolling.ts & 10 & Poll creation, one-nullifier-one-vote enforcement, and finalization \\
    \hline
    AuthorityMultiSig.ts & 4 & Super Admin and authority proposals under majority-vote governance \\
    \hline
    Total & 75 & All four smart contracts \\
    
\end{tabular}
\end{table}

\subsection{Functional Test Procedure}

For Reporting.sol, both valid and deliberately invalid reports are submitted, exercising every custom revert condition alongside the success path. Validation, verification, and rejection-review votes are cast from multiple accounts, voting deadlines are advanced, and the resulting status transition has been checked against the state machine defined in Chapter~\ref{ch:architecture}. Pagination, per-citizen lookup, and the authority action history has been exercised so that the read-side of the contract is verified as same as the write-side.

For EmergencyReporting.sol, the fast-track path has been checked end to end. A submission opens immediately without a validation phase, an authority can resolve it directly from Open or after claiming it with startWork, and a reclassified report correctly locks its citizen's pseudonym for the configured penalty window. For OpinionPolling.sol, poll creation restrictions, nullifier-based one-vote enforcement, and deadline handling has been covered. For AuthorityMultiSig.sol, proposal creation, automatic self-approval, and quorum-based execution ahas been covered for both Super Admin and authority-worker changes.
\subsection{Results}

\begin{table}[htbp]
\centering
\caption{Smart-contract test results}
\label{tab:contract-results}
\begin{tabular}{p{0.35\textwidth}|p{0.20\textwidth}|p{0.33\textwidth}}
    
    Suite & Cases run & Outcome \\
    \hline
    Reporting.ts & 41 & All passed \\
    \hline
    EmergencyReporting.ts & 13 & All passed \\
    \hline
    OpinionPolling.ts & 10 & All passed \\
    \hline
    AuthorityMultiSig.ts & 4 & All passed \\
    \hline
    Security.ts & 7 & All passed \\
    
\end{tabular}
\end{table}


All 75 cases across the five contract test files passed in a single combined Hardhat run completing in five seconds. More details on the test cases are provided in Appendix~\ref{apndx:smart-contract-test-cases}.

\section{Security and Sybil-Resistance Testing}
\label{sec:security-testing}

\subsection{Method}

Security.ts is dedicated to the specific risks a public-facing civic-reporting system has to withstand. A citizen or bot retrying the same action, a citizen attempting to use more than one identity to influence a vote, and an authority action being triggered by an unauthorized account are some examples for such risks. Table~\ref{tab:security-cases} summarizes the six checks in this suite, each targeting a distinct attack surface identified during the architecture design in Chapter~\ref{ch:architecture}.

\begin{table}[htbp]
\centering
\caption{Security and Sybil-resistance test cases}
\label{tab:security-cases}
\begin{tabular}{p{0.10\textwidth}|p{0.80\textwidth}}
    
    Case & What is checked \\
    \hline
    T6.1 & A used submission nullifier is replayed 100 times in a row; every attempt is expected to revert \\
    \hline
    T6.2 & A non-relayer account cannot call submitReport directly \\
    \hline
    T6.3 & A second validation vote from the same citizen pseudonym is rejected even when a fresh vote nullifier is used \\
    \hline
    T6.4 & A second poll vote with the same nullifier is rejected \\
    \hline
    T6.5 & A reclassified emergency report locks its citizen's pseudonym for 30 days, and submission succeeds again once the penalty period has elapsed \\
    \hline
    T6.6 & A second verification vote from the same citizen pseudonym is rejected during the post-resolution phase \\
    
\end{tabular}
\end{table}

Running the replay case 100 times, rather than once, is a deliberate choice. A single successful rejection could be a coincidence of test ordering, whereas 100 consecutive rejections give confidence that the nullifier check is a behaving as designed. The other five cases are each run once, because they are deterministic and do not depend on a sequence of events. More details on the test cases are provided in Appendix~\ref{apndx:security-test-cases}. 

\subsection{Results}

The results of the security and Sybil-resistance tests are summarized in Table~\ref{tab:security-results}. All six cases pass, including all 100 replay attempts in T6.1, completing in 291\,ms.

\begin{table}[htbp]
\centering
\caption{Security and Sybil-resistance test results}
\label{tab:security-results}
\begin{tabular}{p{0.45\textwidth}|p{0.45\textwidth}}
    
    Case & Outcome\\
    \hline
    T6.1 & Passed \\
    \hline
    T6.2 & Passed \\
    \hline
    T6.3 & Passed \\
    \hline
    T6.4 & Passed \\
    \hline
    T6.5 & Passed \\
    \hline
    T6.6 & Passed \\
    
\end{tabular}
\end{table}


\section{Relayer Unit Testing}
\label{sec:relayer-testing}

\subsection{Method}

The relayer's cryptographic and validation logic has been tested in isolation with Jest. The relayer test suite is split into three files. Table~\ref{tab:relayer-inventory} summarizes the 25 cases across these three files.


\begin{table}[htbp]
\centering
\caption{Relayer unit test suite}
\label{tab:relayer-inventory}
\begin{tabular}{p{0.32\textwidth}|p{0.10\textwidth}|p{0.46\textwidth}}
    
    Test file & Cases & Coverage \\
    \hline
    reporting.service.spec.ts & 16 & government ticket verification, citizen payload signature verification, citizen pseudonym derivation, vote payload verification, and environment configuration validation. \\
    \hline
   ai-oracle.service.spec.ts & 8 & canonical JSON hashing and signing logic the relayer uses when it talks to the AI aggregator \\
    \hline
    app.controller.spec.ts & 1 & NestJS application bootstrapping \\
    \hline
    Total & 25 & All three relayer components \\
    
\end{tabular}
\end{table}

  Real Ethers.js wallets has been used to sign and verify messages in these tests, so the actual cryptographic recovery logic has been exercised rather than a mocked implementation.  
\subsection{Results}

\begin{table}[htbp]
\centering
\caption{Relayer unit test results}
\label{tab:relayer-results}
\begin{tabular}{p{0.42\textwidth}|p{0.15\textwidth}|p{0.31\textwidth}}
    
    Spec file & Cases & Outcome \\
    \hline
     {app.controller.spec.ts} & 1 & Passed \\
    \hline
     {ai-oracle.service.spec.ts} & 8 & Passed \\
    \hline
     {reporting.service.spec.ts} & 16 & Passed \\
    \hline
    Total & 25 & All passed \\
    
\end{tabular}
\end{table}


All three spec files pass, giving 25 passing cases in 2.538\,s. More detailed test cases are provided in Appendix~\ref{apndx:relayer-test-cases}.

\section{AI Moderation Testing}
\label{sec:ai-testing}

\subsection{Method}

The AI oracle test suite, shown in Figure~\ref{fig:ai-test-categories}, is split into two files that mirror the two concerns of the moderation service. Those concerns are whether a request is allowed to reach the aggregator at all, and whether the aggregator reaches the correct decision once it does.

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.8\textwidth,height=0.4\textheight,keepaspectratio]{Images/Chapter 5/ai-test-categories.png}
    \caption{Categories covered by the AI oracle test suite}
    \label{fig:ai-test-categories}
\end{figure}


\begin{table}[htbp]
\centering
\caption{AI moderation test suite}
The test suite is split into two files, one for API security and one for classification and moderation as summarized in Table~\ref{tab:ai-test-suite}. 

\label{tab:ai-test-suite}
\begin{tabular}{p{0.32\textwidth}|p{0.10\textwidth}|p{0.46\textwidth}}
    
    Test file & Cases & Coverage \\
    \hline
    {test\_api\_security.py} & 8 & a fully valid request, a missing or wrong API key, a forged relayer signature, a replayed nonce, an oversized file, and a disallowed file count or MIME type.\\
    \hline
    {test\_classification.py} & 12 & Text classification accuracy, media moderation, and response auditability. \\
    \hline
    Total & 20 & All three service components and aggregator \\
    
\end{tabular}
\end{table}


\subsection{Results}

The results of the AI oracle test suite are summarized in Table~\ref{tab:ai-results}. All 20 cases pass, completing in 21.33\,s. More details on the test cases are provided in Appendix~\ref{apndx:ai-test-suite}. Genuine civic and emergency reports has been accepted, while hate speech, spam, and off-topic texthas been rejected. The corrupted and unsafe image cases has been rejected, the safe-image and face-containing cases has been accepted, and every recorded response includes both a decision and an explanation. 

\begin{table}[htbp]
\centering
\caption{AI oracle test results}
\label{tab:ai-results}
\begin{tabular}{p{0.42\textwidth}|p{0.15\textwidth}|p{0.31\textwidth}}
    
    Category & Cases & Outcome \\
    \hline
    API security & 8 & All passed \\
    \hline
    Text classification accuracy & 5 & All passed \\
    \hline
    Media moderation & 4 & All passed \\
    \hline
    Response auditability & 3 & All passed \\
    
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            width=0.82\textwidth,
            height=6cm,
            bar width=22pt,
            symbolic x coords={API Security,Classification,Media,Auditability},
            xtick=data,
            x tick label style={rotate=20, anchor=east, font=\small},
            nodes near coords,
            ymin=0, ymax=10,
            ylabel={Passing test cases},
            enlarge x limits=0.15,
            axis lines*=left,
            clip=false,
        ]
        \addplot[fill=barblue] coordinates {(API Security,8) (Classification,5) (Media,4) (Auditability,3)};
        \end{axis}
    \end{tikzpicture}
    \caption{AI oracle test cases by category (20 total)}
    \label{fig:ai-category-chart}
\end{figure}


\section{Blockchain Performance Benchmark}
\label{sec:performance-benchmark} 

\subsection{Method}

A dedicated Hardhat script measures the private network's practical performance directly against the live running deployed blockchain, rather than relying on theoretical figures for Clique PoA. Figure~\ref{fig:benchmark-measurements} shows the measurements that has been measured.

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.95\textwidth, height=0.6\textheight,keepaspectratio]{Images/Chapter 5/benchmark-measurements.png}
    \caption{Stages of the blockchain performance benchmark}
    \label{fig:benchmark-measurements}
\end{figure}

Gas cost for  {submitReport()} has been measured first, over five submissions, so that a representative average is available before any other activity affects network conditions. Then, End-to-end latency has been measured from the moment a transaction is dispatched to the moment its receipt confirms inclusion in a block, giving a citizen-facing value. Throughput has been measured by dispatching a batch of transactions concurrently, with explicit nonce and fee management so that the batch is genuinely submitted together, and dividing the confirmed count by the elapsed time. The Clique block interval has been read directly from consecutive block timestamps rather than assumed from the configured period, which lets the recorded interval reflect real validator behaviour. Voting gas has been measured the same way as submission gas, and batch-finalization gas has been measured by submitting reports to a short-window contract instance and finalizing them together in one call.

\subsection{Results}

The results of the blockchain performance benchmark are summarized in Table~\ref{tab:performance-results}. More details on the benchmark script and its measurements are provided in Appendix~\ref{apndx:blockchain-performance-test-cases}.

\begin{table}[htbp]
\centering
\caption{Blockchain performance benchmark results}
\label{tab:performance-results}
\begin{tabular}{p{0.40\textwidth}|p{0.25\textwidth}|p{0.25\textwidth}}
    
    Measurement & Recorded value & Notes \\
    \hline
    Avg. gas per  {submitReport()} & 309{,}056 gas & Average of 5 submissions \\
    \hline
    Avg. submit-to-confirm latency & 5{,}017\,ms & Average of 5 submissions (4{,}789--5{,}642\,ms) \\
    \hline
    Throughput (TPS) & 0.87 TPS & Batch of 20 concurrent transactions, 20/20 confirmed in 22{,}956\,ms \\
    \hline
    Avg. Clique block interval & 5.0\,s & Configured period is 5\,s \\
    \hline
    Avg. gas per  {castValidationVote()} & 51{,}806 gas & Average across cast votes \\
    \hline
     {batchFinalizeVotingWindows()} gas & 251{,}909 gas total (25{,}190 gas/report) & Batch of 10 reports \\
    
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            width=0.82\textwidth,
            height=6.2cm,
            bar width=26pt,
            symbolic x coords={submitReport,castValidationVote,batchFinalize per report},
            xtick=data,
            x tick label style={rotate=20, anchor=east, font=\small},
            nodes near coords,
            ymin=0, ymax=340000,
            ylabel={Gas used},
            enlarge x limits=0.25,
            axis lines*=left,
            clip=false,
            every node near coord/.append style={font=\small},
        ]
        \addplot[fill=barblue] coordinates {(submitReport,309056) (castValidationVote,51806) (batchFinalize per report,25190)};
        \end{axis}
    \end{tikzpicture}
    \caption{Recorded gas cost by contract operation}
    \label{fig:gas-cost-chart}
\end{figure}

\begin{figure}[!htb]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            width=0.82\textwidth,
            height=6cm,
            xlabel={Report index in the T5.2 measurement},
            ylabel={Submit-to-confirm latency (ms)},
            xtick={5,6,7,8,9},
            ymin=0, ymax=6500,
            legend pos=south east,
            axis lines*=left,
        ]
        \addplot[color=blue, mark=*] coordinates {(5,4877) (6,4876) (7,4789) (8,4900) (9,5642)};
        \addlegendentry{Recorded latency}
        \addplot[dashed, color=red] coordinates {(5,5000) (9,5000)};
        \addlegendentry{Configured 5\,s block period}
        \end{axis}
    \end{tikzpicture}
    \caption{Submit-to-confirm latency for the five T5.2 report submissions}
    \label{fig:latency-chart}
\end{figure}



These measurements has been read directly from the deployed network described in Table~\ref{tab:evaluation-environment}, using the live Geth Clique PoA network rather than a locally run blockchain.

\section{IPFS and Identity Simulator Checks}
\label{sec:ipfs-identity-testing}

\subsection{Method}

A manual integration script uploads files of increasing size through the IPFS HTTP service, retrieves each one by its returned CID, and compares the retrieved content hash against the original. This directly exercises the content-addressing guarantee the architecture relies on. If a CID resolves to content whose hash matches what was uploaded, the integrity property is behaving as designed. Concurrent uploads and an unreachable-service case are also checked, so that both normal and failure conditions are covered.

\subsection{Results}

The test cases, their descriptions, and the results of the IPFS integration tests are summarized in Table~\ref{tab:ipfs-results}. More details on the test cases are provided in Appendix~\ref{apndx:ipfs-test-suite}.

\begin{table}[htbp]
\centering
\caption{IPFS integration test results}
\label{tab:ipfs-results}
\begin{tabular}{p{0.14\textwidth}|p{0.46\textwidth}|p{0.30\textwidth}}
    
    Case & Description & Recorded outcome \\
    \hline
    T4.1 & Text-only complaint upload ($\sim$1\,KB) & Passed in 518\,ms \\
    \hline
    T4.2 & Complaint upload with $\sim$100\,KB image & Passed in 1{,}034\,ms \\
    \hline
    T4.3 & Complaint upload with $\sim$1\,MB image & Passed in 1{,}331\,ms \\
    \hline
    T4.4 & Complaint upload with $\sim$5\,MB image & Passed in 4{,}308\,ms \\
    \hline
    T4.5 & Returned CID is a valid IPFS format & Passed \\
    \hline
    T4.6 & Poll metadata upload to IPFS & Passed in 272\,ms \\
    \hline
    T4.7 & Unreachable IPFS service correctly throws an error & Passed \\
    \hline
    T4.8 & Five concurrent uploads, no CID collisions & Passed in 3{,}048\,ms (5/5 succeeded) \\
    
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            width=0.8\textwidth,
            height=6cm,
            bar width=26pt,
            symbolic x coords={1KB,100KB,1MB,5MB},
            xtick=data,
            nodes near coords,
            ymin=0, ymax=5000,
            ylabel={Upload latency (ms)},
            enlarge x limits=0.25,
            axis lines*=left,
            clip=false,
        ]
        \addplot[fill=barblue] coordinates {(1KB,518) (100KB,1034) (1MB,1331) (5MB,4308)};
        \end{axis}
    \end{tikzpicture}
    \caption{IPFS upload latency by attachment size (T4.1--T4.4)}
    \label{fig:ipfs-latency-chart}
\end{figure}



All eight cases has been passed. As illustrated in Figure~\ref{fig:ipfs-latency-chart}, upload latency increases with attachment size, from 518\,ms for a text-only complaint to 4{,}308\,ms for a $\sim$5\,MB image, and five concurrent uploads complete successfully with distinct CIDs.

The identity simulator has been exercised as part of the same integrated workflow described in Section~\ref{sec:e2e-testing}. A government-signed ticket identifier is issued, consumed by the relayer, and its nullifier is recognized by the contracts on reuse.

\section{End-to-End Workflow Scenario}
\label{sec:e2e-testing}

A complete lifecycle scenario has been exercised across every layer of the system at once.  A simulated citizen authenticates and receives a signed ticket, submits a standard report, and observes the ordered progress events described in Chapter~\ref{ch:implementation} as the report moves through AI moderation, IPFS storage, and on-chain submission. Three further simulated citizens then cast validation upvotes, the voting window is finalized, and the report moves to  {Open}. An authority claims the report, posts an update, and marks it solved, moving it to  {PendingVerification}. Two simulated citizens accept the resolution, and the report reaches  {Closed}, completing the full report lifecycle shown in Chapter~\ref{ch:architecture}. The screenshots attached in Appendix~\ref{apndx:e2e} provide visual confirmation of this process.

\section{Chapter Summary}
\label{sec:testing-summary}

This chapter has described how the prototype is tested and recorded the results obtained. 75 smart-contract cases across five test files, 25 relayer unit-test cases across three spec files, 20 AI moderation cases across two test files, and 8 IPFS integration cases, for a combined 128 passing test cases, together with a dedicated performance benchmark on the live private network and a full end-to-end lifecycle scenario. The recorded benchmark values include an average of 309{,}056 gas per  {submitReport()}, a 5{,}017\,ms average submit-to-confirm latency, 0.87 TPS on a 20-transaction batch, and a 5.0\,s average Clique block interval. The complete, itemized list of individual test cases is provided in Appendix~\ref{app:test-cases} for full traceability. What these results mean for the project's stated objectives is discussed next in Chapter~\ref{ch:results}.
